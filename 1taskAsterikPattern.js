

function pattern(){
let asterik = '';

  for(let i = 1; i <= 5; i++) {
    asterik += '* ';
    // console.log(asterik);
    console.log(asterik, `ini looping ke ${i}`); //proses looping
  }
}

pattern(5);
